import * as React from 'react';
import wrapIcon from '../utils/wrapIcon';
const rawSvg = (iconProps) => {
    const { className, primaryFill } = iconProps;
    return React.createElement("svg", { width: 24, height: 24, viewBox: "0 0 24 24", xmlns: "http://www.w3.org/2000/svg", className: className },
        React.createElement("path", { d: "M4 13h16a1 1 0 100-2H4a1 1 0 100 2z", fill: primaryFill }));
};
const Subtract24Filled = wrapIcon(rawSvg({}), 'Subtract24Filled');
export default Subtract24Filled;
